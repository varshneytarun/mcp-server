{
  "name": "mcp-api-server",
  "version": "1.0.0",
  "description": "MCP server for calling multiple APIs with path parameters",
  "main": "src/server.js",
  "bin": {
    "mcp-api-server": "./src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mcp",
    "api",
    "server",
    "model-context-protocol"
  ],
  "author": "Tarun Varshney",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}